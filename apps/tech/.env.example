# apps/tech — Environment Variables
# Copy to .env.local. NEVER commit .env.local.
# All variables confirmed from Vercel production env (2026-02-21).

# ── Sanity CMS ──────────────────────────────────────────────────────────────
# Project: SB-Lang-Glob (oaQatKgxT)
# Dataset: production

NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production

# Server-side only — never exposed to browser
# In Vercel: named SANITY_API_TOKEN (Viewer role)
# For CI schema deploy: use a separate Editor token named "GitHub CI Schema Deploy"
SANITY_API_TOKEN=

# Preview and revalidation — add when webhook is configured
SANITY_PREVIEW_SECRET=
SANITY_REVALIDATE_SECRET=

# ── Lokalise i18n ────────────────────────────────────────────────────────────
# Lokalise is the translation management platform for SB-Lang-Glob.
# packages/i18n in sb-web is built around Lokalise, not a custom i18n layer.
# See packages/i18n/README.md for architecture.

LOKALISE_API_TOKEN=
LOKALISE_PROJECT_ID=
