# ==========================================================
# SB/66 INTERNAL USE ONLY — CONFIDENTIAL
# Do not share externally. Do not commit real secrets.
# This file is a template; values here are placeholders only.
# ==========================================================

# --------------------------
# Runtime selection
# --------------------------
NODE_ENV=development
NEXT_PUBLIC_APP_ENV=development

# --------------------------
# Sanity (Tech app CMS)
# --------------------------
# Public (safe to expose to browser if needed)
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2025-01-01

# Optional: if you use Sanity CDN for reads
NEXT_PUBLIC_SANITY_USE_CDN=true

# Private tokens (NEVER expose to browser; never commit real values)
# Vercel Production Read-only token (Viewer)
SANITY_READ_TOKEN=__REPLACE_ME__

# GitHub CI Schema Deploy token (Editor)
SANITY_CI_DEPLOY_TOKEN=__REPLACE_ME__

# --------------------------
# Lokalise (i18n platform)
# --------------------------
# Public-ish identifiers (safe)
NEXT_PUBLIC_LOKALISE_PROJECT_ID=your_lokalise_project_id

# Private token for CI sync (NEVER expose to browser)
LOKALISE_API_TOKEN=__REPLACE_ME__

# Optional: If you map to your "SB-Lang-Glob" name, keep it as a non-secret label
SB_LANG_GLOB_NAME=SB-Lang-Glob

# --------------------------
# CI / GitHub
# --------------------------
# If needed for workflows (prefer GitHub Secrets instead of env files)
GITHUB_TOKEN=__SET_IN_CI_ONLY__

# --------------------------
# Security / Observability (optional)
# --------------------------
# SENTRY_DSN=__REPLACE_ME__
# NEXT_PUBLIC_SENTRY_DSN=__REPLACE_ME__
